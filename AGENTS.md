# Repository Workflow

Per ogni modifica al progetto, dopo la verifica necessaria, è obbligatorio
creare un commit e caricarlo su GitHub con `git push`. Non lasciare modifiche
completate soltanto nella copia locale.
